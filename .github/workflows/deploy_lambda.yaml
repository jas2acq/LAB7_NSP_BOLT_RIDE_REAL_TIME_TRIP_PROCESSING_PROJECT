name: Deploy Lambda Functions

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/deploy_lambda.yaml' 
      - 'src/lambda/**'
  workflow_dispatch:

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}